[package]
name = "namma-diesel"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde_norway = "0.9"
heck = "0.5"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
